<li class='list'>
    <%= link_to '#' do %>
    <div class='drawing-img-content'>
      <%= image_tag(drawing.image, class: "item-img") if drawing.image.attached? %>
    </div>
    <div class='item-info'>
      <h3 class='item-name'>
        <%= drawing.title %>
      </h3>

      <% if drawing.price %>
        <div class='item-price'>
          <span><%= drawing.price %>円</span>
        </div>
      <% end %>  
    </div>
    <% end %>
</li>